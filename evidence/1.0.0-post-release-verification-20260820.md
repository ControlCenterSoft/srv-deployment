# Control Center 1.0.0 — post-release verification

Дата проверки: 2026-08-20

Статус: **VERIFIED / NO NEW SERVER ACTION REQUIRED**

## Accepted immutable identity

- Version: `1.0.0`
- Frozen source commit: `1b364ae88789696bf98537d21544de8a259d086d`
- Built at: `2026-08-20T16:07:56Z`
- linux/amd64 SHA-256: `341f6a49646f92aca72692730ea61278cc03bfc1689d029d7a7ce573554fa735`
- linux/arm64 SHA-256: `5e6e959f39bd4cb554e902908863c92eb5c697f4d99b55ca97fa4735ec9419fe`

## Frozen candidate status checks

Повторно проверены commit statuses exact frozen SHA. Все шесть release-контекстов остаются `success`:

- `control-center/stable-builder-inspect`
- `control-center/stable-real-host-builder`
- `control-center/stable-real-host-final`
- `control-center/1.0.0-stable-ci`
- `control-center/stable-real-host-safe-builder`
- `control-center/stable-real-host-safe-final`

## Production acceptance and canonical release state

Production closure evidence фиксирует real-host acceptance `PASSED` на `ruvds-pr1re` и immutable release identity выше.

Текущий `main` head на момент проверки: `49b7753d0acaa9f0e9e27a29e54a8884fa6147e4` (`Зафиксировать post-release guard для принятого Control Center 1.0.0`). Его родитель `54dd437b4109d6a4f48c4af9f968d7004fc9685e` интегрировал принятый 1.0.0 в `main`.

Канонический `deployment.json` в `main` фиксирует:

- `version=1.0.0`
- `channel=stable`
- `status=production-ready`
- `acceptance=passed`
- exact `source_commit`, `built_at` и обе artifact SHA-256 accepted candidate.

## Post-release guard

`main` содержит fail-closed post-release workflow, который:

- требует, чтобы accepted source commit оставался ancestor текущего `main`;
- разрешает post-release drift только для metadata/CI closure paths;
- запрещает незаметный runtime/tooling drift относительно accepted source;
- повторно собирает exact accepted commit и проверяет принятые artifact SHA-256;
- проверяет канонический production manifest.

## Diagnostics / gateway

Ветка `ops/chatssh-results` на момент этой проверки не содержит опубликованного результата bootstrap/self-repair jobs. Это не влияет на закрытый release gate 1.0.0, поскольку production real-host acceptance уже имеет подтвержденный PASS. ChatSSH gateway остаётся отдельной operational-задачей и не меняет accepted runtime identity.

## Governance decision

Release gates 1.0.0 закрыты. Дополнительный launcher или действие на тестовом сервере для release closure сейчас не требуется.

Эта запись создана только в отдельной evidence-ветке. `main`, PR и merge этой проверкой не изменялись.
