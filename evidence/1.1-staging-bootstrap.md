# Control Center 1.1.x — staging bootstrap evidence

Status: bootstrap executed on the real test server.

Repository-level auto-merge is enabled. The next integration push into `1.1.x` is used as the authoritative verification that the configured `CONTROL_CENTER_STAGING_*` repository secrets are visible to GitHub Actions and that the signed RC can pass the real-server staging gate.

No secret values, private keys, tokens, or host credentials are recorded in this file.
