# Core A 1.2.x — bounded operation incidents evidence

## Exact development context

- Repository: `ControlCenterSoft/srv-deployment`.
- Initial pre-edit base: `1.2.x@f1a2d4517d52f47014502c60103ee59c03347cc3`, the Integrator merge of Core A PR #220.
- While this slice was being prepared, `1.2.x` advanced through Core B DNS PR #217 to `dd715d7d3260db4eeccb50dbfe626c2985cf2051`, then through Experience Admin Web DNS PR #216 to `f39aef625a0eb53106100505ea271b9c90094aef`.
- Both intervening merges were inspected before synchronization: #217 owns `internal/dns/**` plus DNS evidence; #216 owns `internal/httpserver/web/network.js`, its web DNS contract test, and DNS evidence. Neither overlaps this Core A slice.
- Final synchronized base for the feature branch remains `1.2.x@f39aef625a0eb53106100505ea271b9c90094aef`.
- Before the route-compatibility correction, canonical `1.2.x` advanced to `bac25422e2987481f355194f1f9a25582c00fa6a`. The exact `f39aef6..bac2542` compare changes only network address-preflight, network tests/evidence, and Web visual CSS/contract files; it does not overlap the Operations/Incidents files in this PR. Integrator still owns any later base synchronization before merge.
- Slice: one read-only Operations/Incidents projection over the existing version-1 operation store.
- Branch: `feature/operations-incidents-20260822`.

Pre-edit WIP inspection found no open Core A feature PR. Active server PRs own release-gate/workflow files, Core B network/DNS files, Admin Web files, or Visual UI CSS/test files. None owns `internal/operations/**`, `internal/httpserver/operations_detail.go`, the incidents implementation/tests, or this evidence path.

## User-facing platform contract

`GET /api/v1/operations/_incidents` exposes a bounded newest-first feed of abnormal terminal operations so an operator does not need to scan successful/running history to identify failures.

The collection uses reserved path ID `_incidents`. That identifier is deliberately outside the accepted operation-detail ID regex (`^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$`), so every previously valid detail ID — including literal `incidents` — remains available at `GET /api/v1/operations/{id}`. This closes the route collision found by independent Contract/Regression QA child #238 without narrowing or redefining the existing detail-ID contract.

The additive response keeps the existing operation record schema and declares:

- `contract_version=1`;
- `incidents`: only `failed` and `interrupted` operation records;
- `count`, `bounded=true`, and the effective `limit`;
- `statuses=["failed","interrupted"]`;
- `permissions.read=operations.read`;
- operation-store and audit-log health metadata;
- `observed_at`.

The default limit is 50. Explicit limits are strict and bounded to 1..100. Malformed, zero, negative, duplicate, empty, whitespace-only, or over-limit values fail with stable HTTP 400 `invalid_limit`; they are not silently rewritten.

## Security, RBAC and audit semantics

- Authentication remains server-side and the existing `operations.read` permission remains authoritative.
- Viewer/unauthorized access cannot read incident records; authenticated permission denial is audited as `operations.incidents.read` / `permission_denied`.
- Successful incident reads are audited as `operations.incidents.read`.
- Invalid-limit requests from an authenticated authorized caller are audited with `invalid_limit`.
- The projection reuses the existing safe operation schema and contains no request body, password, session cookie/token, CSRF token, or other credential material.
- The reserved `_incidents` collection identifier is not accepted as an operation detail ID; no valid detail identifier is consumed by the collection.
- No mutation, privileged execution, Desired/Actual state change, Fleet mutation, Core B behavior, Admin Web behavior, release identity, deployment, or iOS behavior is introduced.

## Failure and health behavior

- If the operation store is unavailable, the endpoint fails closed with HTTP 503 `operation_store_unavailable`.
- Missing audit wiring does not fabricate audit evidence; the read remains available but reports degraded audit health.
- Existing restart recovery remains authoritative: an unfinished `running` operation is converted by the store to `interrupted` with `process_restarted`, and therefore appears in the incident feed.

## Acceptance coverage

Deterministic tests cover:

1. failed/interrupted-only filtering;
2. newest-first ordering and bounded limits;
3. restart-recovered `interrupted` visibility with `process_restarted`;
4. successful/running operation exclusion;
5. API contract, permission and health metadata;
6. strict `limit` validation and stable `invalid_limit` error;
7. anonymous and viewer denial;
8. audit evidence for invalid input and permission denial;
9. credential non-disclosure in the incident response;
10. route compatibility: literal valid detail ID `incidents` remains in the operation-detail namespace, while reserved `_incidents` is outside that namespace.

Independent QA child #238 was created against parent head `c99d16226bcc9c17b84e7547593a6a28edb2c5c3` and correctly failed because the first implementation special-cased valid detail ID `incidents`. Its regression fixture has been integrated into the parent branch as `operations_incidents_route_contract_test.go`; exact-head Fast CI plus fresh Contract/Regression QA and Security must be rerun after this correction.

Applicable exact-head gates before Integrator merge:

- `gofmt` / `go vet`;
- `go test ./internal/operations ./internal/httpserver` plus repository unit/contract suite;
- `Control Center 1.2.x Fast CI`;
- exact-head Contract & Regression QA;
- exact-head independent Security Review.

Merge, release and deployment remain owned by `CC Integrator, Release & Docs`.
