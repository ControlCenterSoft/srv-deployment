name: Ops upgrade ruvds-pr1re

on:
  pull_request:
    branches: [1.1.x]
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  upgrade:
    uses: ./.github/workflows/acceptance-1.1.yml
    with:
      candidate_sha: ae0c2dbf5786aebe933e1f779659dda3eafe5e59
      candidate_version: 1.1.0-rc.109
    secrets: inherit

# One-shot operational trigger; do not merge.
